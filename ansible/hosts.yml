---
all:
  hosts:
    # Replace with your actual host IP/hostname
    wifiportal-host:
      ansible_host: 192.168.1.100
      ansible_user: wifiportal
      # ansible_ssh_private_key_file: ~/.ssh/your_key
  vars:
    ansible_python_interpreter: /usr/bin/python3
